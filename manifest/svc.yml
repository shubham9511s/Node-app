apiVersion: v1
kind: Service
metadata:
  labels:
    app: node
  name: node
  namespace: node-app
spec:
  ports:
  - port: 4200
    protocol: TCP
    targetPort: 4200
  selector:
    app: node 
  type: NodePort